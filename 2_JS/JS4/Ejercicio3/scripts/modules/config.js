export const viewConfig = {
  fetch: {
    buttonSelector: "#fetch-load",
    inputSelector: "#fetch-search",
    statusSelector: "#fetch-status",
    resultsSelector: "#fetch-results",
    successMessage: "Usuarios listos para buscar con fetch.",
    errorMessage: "No se pudieron cargar los usuarios con fetch."
  },
  axios: {
    buttonSelector: "#axios-load",
    inputSelector: "#axios-search",
    statusSelector: "#axios-status",
    resultsSelector: "#axios-results",
    successMessage: "Usuarios listos para buscar con axios.",
    errorMessage: "No se pudieron cargar los usuarios con axios."
  }
};
